services:
  - type: web
    name: toxscanbot
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python Bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
